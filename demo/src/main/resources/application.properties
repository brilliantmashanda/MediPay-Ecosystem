spring.application.name=demo

# Database Configuration
#spring.datasource.url=jdbc:postgresql://localhost:5432/MediPay
spring.datasource.url=jdbc:postgresql://db:5432/MediPay
# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Port configuration (to avoid conflict with Python later)
server.port=8080